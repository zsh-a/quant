"""Brooks v2 — rule-first, state-rich price-action strategy.

Importing this package triggers registration of ``brooks_v2`` in
``StrategyRegistry``.
"""

from . import strategy  # noqa: F401  — triggers registry decorator
